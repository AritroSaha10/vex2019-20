#include "autonSelector.h"

void autonSelector() {
	lv_obj_t * scr = lv_obj_create(NULL, NULL);
	lv_scr_load(scr);

	lv_obj_t * red1 = lv_btn_create(lv_scr_act(), NULL);
	lv_obj_set_pos(red, 10, 10);

	
}
